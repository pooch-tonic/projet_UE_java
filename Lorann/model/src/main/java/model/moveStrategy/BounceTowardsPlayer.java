package model.moveStrategy;

import modelInterfaces.IMoveStrategy;

public class BounceTowardsPlayer implements IMoveStrategy {

	/**
	 * strategy which makes the unit bounce towards the player if possible
	 */
	@Override
	public void move() {
		// TODO move strategy #2
	}
}
