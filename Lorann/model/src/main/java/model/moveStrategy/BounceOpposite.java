package model.moveStrategy;

import modelInterfaces.IMoveStrategy;

public class BounceOpposite implements IMoveStrategy {

	/**
	 * strategy which makes the unit bounce towards its opposite direction
	 */
	@Override
	public void move() {
		// TODO move strategy #1
	}
}
